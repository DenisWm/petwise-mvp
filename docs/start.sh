# Note: executable helper scripts must not be stored inside `docs/`.
# If the repository needs helper scripts for local preview or maintenance, create
# a top-level `scripts/` directory (e.g., `/scripts/preview-docs.sh`) and add a
# brief README describing their purpose. Documentation should reference those
# scripts but must not contain runnable code. This file intentionally contains
# no executable commands.

